Start a new project based on this description: $ARGUMENTS

Follow these steps in order:

1. Enter plan mode and produce a plan for scaffolding the project — list every file that will be created and every package that will be installed.

2. Explicitly invoke the dependency-check skill: read the plan, detect the platform, check that all required system tools are present (node, npm, python, git as relevant), and report what's ready and what's missing.

3. Present the plan and the environment status together in one clear summary.

4. Stop and wait for my approval before writing any files or running any install commands.
